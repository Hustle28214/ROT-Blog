import WordCount from '../../../../../../src/components/WordCount/WordCount';
import Mermaid from '@theme/Mermaid';

<WordCount>

## deque

Deque也就是双端队列，可以让用户从队列的头部和尾部进行插入和删除操作。

当你需要一个既支持高效随机访问，又需要在序列两端频繁进行插入和删除操作的容器时，deque 是首选。

</WordCount>