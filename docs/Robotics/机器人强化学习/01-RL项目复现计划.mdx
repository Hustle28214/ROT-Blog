import WordCount from '../../../src/components/WordCount/WordCount';

<WordCount>

## 1. 这个计划的目的

这个应该算是学习的过程，打算先复现一些知名的经典的Robotics强化学习项目。然后再自己构建一些想做的尝试。

构建自己的RL项目的目标，是针对不同的构型去做的，并且我目前也有一些开发板的储备，可以去做不同量级和不同层级的部署。

## 2. 计划复现的项目列表






</WordCount>