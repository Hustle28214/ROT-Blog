import WordCount from '../../../../src/components/WordCount/WordCount';
import Mermaid from '@theme/Mermaid';

<WordCount>

## 分块

分块是一种思想，而不是一种具体的数据结构。

对数据适当进行分块，对每一个划分后的块预处理部分信息。可以把暴力算法的复杂度适当降低。

分块的块长是固定块长或者可变块长。时间复杂度就取决于这个块长，一般可以通过均值不等式找到最优块长。



</WordCount>