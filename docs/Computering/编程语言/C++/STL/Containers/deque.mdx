import WordCount from '../../../../../../src/components/WordCount/WordCount';
import Mermaid from '@theme/Mermaid';

<WordCount>

## deque

Deque也就是双端队列，可以让用户从队列的头部和尾部进行插入和删除操作。

```cpp
#include <deque>
std::deque<T> dq;  // T 为元素类型
```

deque支持 `operator[]` 和 `at()`。当你需要一个既支持高效**随机访问**，又需要在序列两端频繁进行插入和删除操作的容器时，`deque` 是首选。

如果你需要在序列中间频繁插入或删除元素，`list` 或 `forward_list` 会更合适。

如果你需要所有元素都存储在一块连续的内存中（例如，为了与C风格API交互），那么必须使用 `vector`。

本质上，deque是一个动态数组。

```cpp
#include <deque>
std::deque<int> temp;// 初始化一个空的deque

std::deque<int> tempA(2);// 初始化2个0的deque

std::deque<int> tempB(6,10); // 初始化6个10的deque

std::deque<int> d4 = {1, 2, 3, 4, 5}; // 初始化5个元素，1 2 3 4 5的deque
std::deque<int> d5{1, 2, 3, 4, 5}; // 语义与上面相同

std::vector<int> d6 = {1,2,3};
std::deque<int> d7(d6.begin(),d6.end());// 通过迭代器范围构造

std::deque<int> d7(d4);
std::deque<int> d8(std::move(d4)); // 拷贝/移动构造
```

## 常用成员函数

| 函数 | 说明 |
|------|------|
| **元素访问** | |
| `operator[](size_t pos)` | 返回指定位置的引用，不进行边界检查 |
| `at(size_t pos)` | 返回指定位置的引用，带边界检查（越界抛出 `std::out_of_range`） |
| `front()` | 返回首元素的引用 |
| `back()` | 返回尾元素的引用 |
| **迭代器** | |
| `begin()` / `end()` | 返回起始和尾后迭代器 |
| `rbegin()` / `rend()` | 返回反向迭代器 |
| **容量** | |
| `empty()` | 判断是否为空 |
| `size()` | 返回元素个数 |
| `max_size()` | 返回最大可能元素个数 |
| `shrink_to_fit()` | （C++11）请求释放未使用的内存（非强制） |
| **修改器** | |
| `clear()` | 清空所有元素 |
| `insert(pos, value)` | 在指定位置插入元素（可插入多个） |
| `emplace(pos, args...)` | 在指定位置就地构造元素 |
| `erase(pos)` / `erase(first, last)` | 删除指定位置或范围的元素 |
| `push_front(value)` | 在头部插入元素 |
| `emplace_front(args...)` | 在头部就地构造元素 |
| `pop_front()` | 删除头部元素 |
| `push_back(value)` | 在尾部插入元素 |
| `emplace_back(args...)` | 在尾部就地构造元素 |
| `pop_back()` | 删除尾部元素 |
| `resize(count)` / `resize(count, value)` | 改变大小（多删少补） |
| `swap(other)` | 交换两个 deque 的内容 |

`std::deque`也就是双端队列，可以让用户从队列的头部和尾部进行插入和删除操作，同时可以随机访问（`push_front`、`pop_front`、`push_back`、`pop_back`）。

支持通过下标 `[]` 或 `at()` 直接访问任意位置的元素，时间复杂度为`O(1)`。

```cpp
#include <iostream>
#include <deque>

int main() {
    std::deque<int> dq;

    // 尾部插入
    dq.push_back(1);
    dq.push_back(2);
    // 头部插入
    dq.push_front(0);
    dq.push_front(-1);

    // 随机访问
    std::cout << dq[2] << std::endl;   // 输出 1

    // 遍历
    for (auto x : dq) {
        std::cout << x << " ";          // -1 0 1 2
    }
    std::cout << std::endl;

    // 删除头部和尾部
    dq.pop_front();
    dq.pop_back();                       // 现在为 0 1

    return 0;
}
```

## 实现原理

deque 的典型实现是**分段连续线性空间**。它由一个中控器（map） 和多个缓冲区（buffer） 组成：

- 缓冲区（buffer）：是固定大小的连续内存块，通常大小由实现定义（例如 512 字节）。每个缓冲区存储一定数量的元素。

- 中控器（map）：是一个指针数组（或类似结构），每个元素指向一个缓冲区。当需要扩容时，只需向 map 中添加新的缓冲区指针，而不必移动已有元素。

这种设计使得 deque 在逻辑上看起来是连续的，但物理上是分段的。因此，在两端插入删除时，只需要在头部或尾部缓冲区操作，或者当该缓冲区满时，分配新的缓冲区并更新中控器，无需移动其他元素。随机访问时，通过中控器和偏移量计算元素位置：首先确定元素位于哪个缓冲区，然后在该缓冲区内的偏移量。

优点是，避免了 vector 在头部插入/删除时的大量元素移动，也避免了 list 无法随机访问的缺点。

缺点是，随机访问比 vector 稍慢（因为多了一次间接寻址），且内存占用略高（需要中控器开销）。

<Mermaid
value={`
graph LR;
    subgraph map [中控器 map（指针数组）];
        direction LR;
        ptr0(("ptr0"));
        ptr1(("ptr1"));
        ptr2(("ptr2"));
        ptr3(("ptr3"));
    end;

    subgraph buffers [缓冲区 buffer（固定大小内存块）];
        direction LR;
        buf0["[0, 1, 2]"];
        buf1["[3, 4, 5]"];
        buf2["[6, 7, 8]"];
        buf3["[9, 10, 11]"];
    end;

    ptr0 --> buf0;
    ptr1 --> buf1;
    ptr2 --> buf2;
    ptr3 --> buf3;

    buf0 === buf0;
    buf3 === buf3;

    linkStyle 0,1,2,3 stroke:#666,stroke-dasharray:5 5;
    subgraph logical [逻辑视图（连续地址空间）];
        direction LR;
        logical0(("0"));
        logical1(("1"));
        logical2(("2"));
        logical3(("3"));
        logical4(("4"));
        logical5(("5"));
        logical6(("6"));
        logical7(("7"));
        logical8(("8"));
        logical9(("9"));
        logical10(("10"));
        logical11(("11"));
    end;
    logical0 ~~~ logical1 ~~~ logical2 ~~~ logical3 ~~~ logical4 ~~~ logical5 ~~~ logical6 ~~~ logical7 ~~~ logical8 ~~~ logical9 ~~~ logical10 ~~~ logical11;
`}
/>

## 常用方法

| 操作 | 时间复杂度 | 说明 |
|------|------------|------|
| push_back | 均摊 O(1) | 在尾部添加元素，若尾部缓冲区有空间则直接插入，否则分配新缓冲区 |
| push_front | 均摊 O(1) | 在头部添加元素，若头部缓冲区有空位则插入，否则分配新缓冲区 |
| pop_back | O(1) | 删除尾部元素，若尾部缓冲区变空可能释放 |
| pop_front | O(1) | 删除头部元素，若头部缓冲区变空可能释放 |
| insert / erase（中间） | O(n) | 在中间插入或删除元素需要移动该位置之后的元素，与 vector 类似 |
| operator[] / at() | O(1) | 随机访问，通过计算缓冲区位置和偏移量 |
| size() / empty() | O(1) | 通常维护计数器 |
| clear() | O(n) | 销毁所有元素，可能释放缓冲区 |


## 迭代器失效情况

deque 的迭代器失效规则比 vector 复杂，但比 list 更严格。在首尾两端进行插入或删除时，只有指向被操作元素的迭代器失效，其他迭代器（包括指向另一端元素的）仍然有效。但在某些实现中，如果插入导致缓冲区重新分配，则所有迭代器都可能失效（标准未强制规定，但常见实现是只影响首尾端，不影响另一端）。在中间插入或删除元素时，所有迭代器都会失效（因为元素移动会导致迭代器指向的地址变化），类似于 vector。

因此，在使用 deque 时，如果需要对两端操作并保持迭代器有效，应避免在中间插入删除。

## 功能对比

| 容器 | 随机访问 | 头部插入/删除 | 尾部插入/删除 | 中间插入/删除 | 内存布局 | 迭代器失效规则 |
|------|----------|---------------|---------------|----------------|----------|----------------|
| vector | O(1) | O(n) | 均摊 O(1) | O(n) | 单块连续内存 | 重分配时全部失效；插入/删除点之后迭代器失效 |
| deque | O(1) | 均摊 O(1) | 均摊 O(1) | O(n) | 多块连续内存（分段连续） | 插入/删除点之前的迭代器可能有效，但通常两端操作不影响已有迭代器（除首尾块变化外） |
| list | 不支持（需遍历） | O(1) | O(1) | O(1)（已知位置） | 双向链表，非连续 | 除被删除元素外，其他迭代器始终有效 |

## 适用场景与注意事项

实现队列（FIFO）时，需要两端操作（push_back + pop_front）或双端队列（DEQ，如滑动窗口）。

需要随机访问，但也要在头部频繁插入删除（如任务调度器）。

缓冲区管理器（例如生产者-消费者模型中的循环缓冲区）。

注意事项：

1. deque 的 size() 可能需要遍历所有缓冲区来计算，但标准要求常数时间，因此实现通常维护大小计数。

2. 由于分段存储，deque 的迭代器、引用和指针在插入删除时的失效规则较复杂，编程时需留意。

3. 当容器元素很大时，deque 的缓存局部性不如 vector（因为元素分布在不同的内存块中），可能影响遍历性能。

</WordCount>